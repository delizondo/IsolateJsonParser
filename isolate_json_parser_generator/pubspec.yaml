name: isolate_json_parser_generator
description: "Isolate Json Parser Generator Project"
version: 0.0.8
homepage: https://delizondo.com
repository: https://github.com/delizondo/IsolateJsonParser

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  build: ^3.0.0
  source_gen: ^3.0.0
  build_config: ^1.1.2
  code_builder: ^4.10.1
  glob: ^2.1.3
  dart_style: ^3.1.1
  isolate_json_parser: ^0.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
